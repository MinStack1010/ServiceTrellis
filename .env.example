HF_TOKEN="hf_REPLACE_WITH_A_NEW_TOKEN"

# Optional: Redis URL for persistent job storage.
# If not set, jobs are stored in-memory and will be lost on server restart.
# Example (local):  redis://localhost:6379/0
# Example (cloud):  redis://user:password@host:6379/0
REDIS_URL=""
